<div id='topmenu'>
  [
  <%= link_to 'Home', '/' %>
  <% if current_user.can_manage_users? %>
    | <%= link_to 'Users', users_path %> 
  <% end %>
  <% if current_user.can_manage? :houses %>
    | <%= link_to 'Houses', houses_path %> 
  <% end %>
  ]
</div>

<%= render :partial => "#{controller_name}/menu" if File.exists?("#{RAILS_ROOT}/app/views/#{controller_name}/_menu.html.erb") %>
